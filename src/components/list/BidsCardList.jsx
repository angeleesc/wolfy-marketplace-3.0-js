import React from "react";

export default function BidsCardList({ bids }) {

    //obtenemos la carta de la subasta
    // luego la renderizamos 

  return (
    <div className="bids-card-list">
      <h3>Mis oferstas chechas</h3>
    </div>
  );
}
